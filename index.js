/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => App);
/*

El proyecto final debe estar desarrollado con la tecnología de React Native 
los requisitos obligatorios son los siguientes:
    - Manejo de componentes y estilos
    - Integrar al menos un servicio de Firebase
    - Navegación
    - Integrar alguna librería que no se haya utilizado en la clase

*/
